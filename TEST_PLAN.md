# CodeInsight 功能测试计划

## 测试环境准备

### 后端环境
```bash
cd backend
python -m venv .venv
.\.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac
pip install -r requirements.txt
```

### 前端环境
```bash
cd frontend
npm install
```

## 测试清单

### 1. 信息展示测试

**测试目标**: 验证项目详情页面不再显示null值

**步骤**:
1. 启动后端服务: `uvicorn app.main:app --reload`
2. 启动前端服务: `npm run dev`
3. 创建或导入一个项目
4. 访问项目详情页面
5. 检查以下字段：
   - 文件数：应显示数字（如无数据显示0）
   - 代码行数：应显示数字（如无数据显示0）
   - 状态：应显示"就绪"/"索引中"/"错误"或"未知"
   - 创建时间：应显示日期或"未知"
   - 项目名称：应显示名称或"未命名项目"
   - 项目路径：应显示路径或"路径未设置"

**预期结果**: 所有字段都有合理的默认值，不再显示null

### 2. 聊天项目关联测试

**测试目标**: 验证聊天功能正确关联当前项目

**步骤**:
1. 确保项目已完成索引
2. 访问项目的"智能问答"标签页
3. 从项目选择器中选择当前项目
4. 发送问题："这个项目的主要功能是什么？"
5. 验证回答是否包含项目相关信息

**预期结果**: AI回答基于项目上下文，不再是通用回答

### 3. 项目摘要测试

**测试目标**: 验证项目摘要生成功能

**步骤**:
1. 确保项目已完成索引
2. 调用API: `GET /api/v1/chat/project-summary/{project_id}?top_k=20`
3. 检查返回的摘要是否包含：
   - 项目概述
   - 技术栈
   - 主要功能
   - 架构信息

**预期结果**: 返回详细的项目摘要，包含技术栈、架构和主要功能

### 4. 版本创建测试

**测试目标**: 验证版本创建功能

**步骤**:
1. 访问项目的"版本对比"标签页
2. 输入版本号：`1.0.0`
3. 输入描述：`初始版本`
4. 点击"创建版本"
5. 验证版本列表中显示新版本

**预期结果**: 版本创建成功，列表中显示版本信息（版本号、文件数、代码行数、创建时间）

### 5. 版本对比测试

**测试目标**: 验证版本对比功能

**步骤**:
1. 创建第一个版本（如 v1.0.0）
2. 修改项目代码（添加或删除一些文件）
3. 创建第二个版本（如 v1.1.0）
4. 在版本对比页面：
   - 选择版本1: v1.0.0
   - 选择版本2: v1.1.0
   - 点击"开始比较"
5. 检查对比结果：
   - 文件数变化
   - 代码行数变化
   - 详细变更列表

**预期结果**: 显示两个版本之间的差异，包括文件数和代码行数的变化

### 6. 会话过滤测试

**测试目标**: 验证聊天会话按项目过滤

**步骤**:
1. 创建项目A的聊天会话
2. 创建项目B的聊天会话
3. 在项目A的聊天页面，验证只显示项目A的会话
4. 切换到项目B的聊天页面，
验证只显示项目B的会话

**预期结果**: 每个项目的聊天页面只显示该项目的会话

### 7. 前端构建测试

**测试目标**: 验证前端可以成功构建

**步骤**:
```bash
cd frontend
npm run build
```

**预期结果**: 构建成功，生成dist目录

### 8. 后端启动测试

**测试目标**: 验证后端可以成功启动

**步骤**:
```bash
cd backend
uvicorn app.main:app --reload
```

**预期结果**: 服务启动成功，访问 http://localhost:8000/docs 可以看到API文档

### 9. 数据库迁移测试

**测试目标**: 验证新表（Version）可以正确创建

**步骤**:
1. 删除现有数据库文件（如果有）
2. 重启后端服务
3. 检查数据库中是否创建了`versions`表

**预期结果**: `versions`表被成功创建

## 自动化测试脚本

### 后端测试
```bash
cd backend
pytest tests/ -v
```

### 前端类型检查
```bash
cd frontend
npm run typecheck
```

### 前端代码检查
```bash
cd frontend
npm run lint
```

## 性能测试

### 大型项目测试
- 测试包含1000+文件的项目
- 验证版本创建速度（应<5秒）
- 验证聊天响应速度（应<3秒）

### 并发测试
- 同时创建多个版本
- 同时进行多个聊天会话
- 验证系统稳定性

## 回归测试

确保新功能不影响现有功能：
1. 项目创建仍然正常
2. 项目导入仍然正常
3. 项目删除仍然正常
4. 功能分析仍然正常
5. 代码解析仍然正常

## 测试数据

### 示例项目
使用当前code-insight项目自身作为测试项目：
- 文件数：~100+
- 代码行数：~10000+
- 技术栈：Python (FastAPI) + TypeScript (React)

## 测试报告模板

### 测试结果
- [ ] 信息展示无null值
- [ ] 聊天正确关联项目
- [ ] 项目摘要生成成功
- [ ] 版本创建成功
- [ ] 版本对比成功
- [ ] 会话过滤正确
- [ ] 前端构建成功
- [ ] 后端启动成功
- [ ] 数据库迁移成功

### 发现的问题
- 问题描述
- 复现步骤
- 预期结果
- 实际结果

### 建议
- 性能优化建议
- 用户体验改进建议
- 功能增强建议

## 测试完成标准

所有测试项目通过后，可以认为功能实现完成，准备进入生产环境。

## 注意事项

1. **API密钥**: 确保配置了有效的LLM API密钥（用于聊天和摘要功能）
2. **数据库**: 首次运行会自动创建新的`versions`表
3. **端口占用**: 确保8000（后端）和3000（前端）端口未被占用
4. **浏览器缓存**: 测试前端时建议清除浏览器缓存

## 持续集成

建议将测试脚本集成到CI/CD流程中：
- 每次提交自动运行后端测试
- 每次提交自动运行前端类型检查
- 每次提交自动运行前端构建
